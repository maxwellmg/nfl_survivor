def weeks_played?()
    teams_selected = []
    puts "Has the NFL Regular Season started? (Answer either 'y' or 'n')"
    resp = gets.chomp
    while length.teams_selected == 0
      if resp == "y"
  
      elsif resp == "n"
        puts "Okay! Starting from scratch then."
  
      else
        puts ""